{
  "name": "server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "build": "python3 -m pip install --upgrade pip && (python3 -m pip install -U yt-dlp || pip install -U yt-dlp) || echo 'pip install bypassed/failed'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^2.0.0",
    "nanoid": "^5.0.7",
    "pg": "^8.22.0"
  }
}
